services:
  - type: web
    name: softsensing-ai-demo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py  # or whatever your Dash entry file is
    pythonVersion: 3.10.12
    envVars:
      - key: PORT
        value: 10000
